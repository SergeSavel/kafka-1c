# Карта проекта

Справочник по составу и устройству репозитория: что где лежит, что за что отвечает и куда смотреть при типовой задаче. Нужен, чтобы не перечитывать конфигурацию целиком ради ответа на частный вопрос.

Дополняет [AGENTS.md](AGENTS.md), а не заменяет его: там правила работы (сборка, соглашения, механика обмена), здесь — инвентарь и навигация.

## Состав конфигурации

В состав подсистемы `Кафка` входят все объекты конфигурации, кроме неё самой. Исходники — проект 1C:EDT в `configuration/`, режим совместимости 8.3.10 (режим совместимости расширений и сам проект EDT — 8.3.27), `scriptVariant = Russian`, управляемое приложение, модальность и синхронные вызовы запрещены.

| Объект | Назначение |
|---|---|
| `CommonModule.КафкаСервер` | Ядро: сценарии обмена, параллелизм, позиции, сериализация |
| `CommonModule.КафкаВызовСервера` | Обёртка `&НаСервере` из трёх процедур-делегатов |
| `CommonModule.КафкаПереопределяемый` | Единственная открытая точка расширения ядра |
| `CommonModule.КафкаПовтИсп` | Кэш метаданных и настроек на время сеанса |
| `DataProcessor.КафкаАдаптер` | HTTP-клиент шлюза, по методу на эндпойнт |
| `DataProcessor.КафкаАдминистрирование` | Рабочее место администратора Kafka |
| `Catalog.КафкаКластеры` | Подключение: адрес шлюза + параметры клиента Kafka |
| `Catalog.КафкаШины` | Один элемент — один однонаправленный обмен |
| `InformationRegister.КафкаПозиции` | Позиции чтения (оффсеты хранятся в 1С, не в Kafka) |
| `Constant.КафкаКластерПоУмолчанию` | Кластер, подставляемый в новые шины и в рабочее место |
| `Enum.КафкаНаправленияОбмена` | `Выгрузка`, `Загрузка` |
| `Enum.КафкаСердес` | `Игнорировать`, `Null`, `Строка`, `ДвоичныеДанные`, `СтруктураJson`, `СоответствиеJson`, `Xml`, `Uuid` |
| `ScheduledJob.КафкаВыгрузка` / `КафкаЗагрузка` | Шаблоны заданий; экземпляры создаются на каждую шину |
| `Role.КафкаАдминистрирование` | Единственная роль, она же `defaultRoles` |
| `Subsystem.Кафка` | Корневая подсистема, выводится в командный интерфейс |
| `CommonPicture.Кафка` | Иконка Apache Kafka в SVG |

Поставка: `Configuration.distr` держит все объекты в `ProtectMode`, подсистему `Кафка` — в `WarningMode`; записи для `КафкаПереопределяемый` нет, поэтому модуль остаётся редактируемым у внедренца.

## Куда смотреть при типовой задаче

| Задача | Файлы |
|---|---|
| Логика выгрузки 1С → Kafka | `КафкаСервер/Module.bsl`, область `АвтоВыгрузка` |
| Логика загрузки Kafka → 1С | `КафкаСервер/Module.bsl`, область `АвтоЗагрузка` |
| Сериализация ключа и значения | `КафкаСервер/Module.bsl`, `СериализоватьКлючВБуферДвоичныхДанных`, `ДесериализоватьЗначение`; контракт — [docs/dev/serdes.md](docs/dev/serdes.md) |
| Функции JSON/XML/Avro для прикладного кода | `КафкаСервер/Module.bsl`, области `СериализацияJSON` / `СериализацияXML` / `СериализацияAvroAsXml` |
| Новый или изменённый эндпойнт шлюза | `КафкаАдаптер/ObjectModule.bsl` |
| Параметры клиента Kafka (`client.id`, SASL, `group.id`) | `КафкаСервер/Module.bsl`, `КонфигурацияБазовая` / `КонфигурацияОтправителя` / `КонфигурацияПолучателя` / `КонфигурацияШины` |
| Валидация настроек шины | `Catalogs/КафкаШины/ObjectModule.bsl`, `ПередЗаписью` |
| Регламентные задания | `КафкаСервер/Module.bsl`, `ОбновитьРегламентныеЗадания` |
| Позиции чтения | `InformationRegisters/КафкаПозиции/ManagerModule.bsl` |
| Рабочее место администратора | `DataProcessors/КафкаАдминистрирование/Forms/Основная/Module.bsl` |
| Поведение в копии базы (бэкапе) | `КафкаПереопределяемый/Module.bsl` + `КафкаСервер.Тема` |

## Что читать целиком, а что нет

Формы (`.form`) — машинный XML, целиком читать почти никогда не нужно: структура (страницы, реквизиты, команды, привязка обработчиков) достаётся `grep` по `name=`. Основная часть смысла — в `.bsl`.

Самые крупные файлы репозитория, в порядке убывания:

| Файл | Чем является |
|---|---|
| `DataProcessors/КафкаАдминистрирование/Forms/Основная/Form.form` | Крупнейший файл репозитория; XML формы, читать только структурно |
| `CommonModules/КафкаСервер/Module.bsl` | Ядро; самый большой модуль |
| `DataProcessors/КафкаАдминистрирование/Forms/Основная/Module.bsl` | Рабочее место |
| `Catalogs/КафкаШины/Forms/ФормаЭлемента/Form.form` | XML формы |
| `Catalogs/КафкаКластеры/Forms/ФормаЭлемента/Form.form` | XML формы |
| `DataProcessors/КафкаАдаптер/ObjectModule.bsl` | Клиент шлюза |
| `Catalogs/КафкаШины/Forms/ФормаЭлемента/Module.bsl` | Диагностика позиций и очереди |
| `Catalogs/КафкаШины/КафкаШины.mdo` | Реквизиты шины |
| `Catalogs/КафкаШины/ObjectModule.bsl` | Вся валидация шины |
| `CommonModules/КафкаПовтИсп/Module.bsl` | Кэш метаданных |

Актуальные размеры, если понадобятся, берутся из индекса — числа в документации устаревают от любой правки кода:

```bash
git -c core.quotepath=false ls-files -z | xargs -0 wc -c | sort -rn | head -20
```

Пустые модули-заглушки (только лицензионная шапка, кода нет): `КафкаАдаптер/ManagerModule.bsl`, `КафкаАдминистрирование/ObjectModule.bsl` и `ManagerModule.bsl`, оба модуля константы, менеджеры обоих перечислений, `КафкаПозиции/RecordSetModule.bsl`, `КафкаКластеры/ManagerModule.bsl`, формы списка и группы.

## Ядро: КафкаСервер

Области модуля: `Общее`, `АвтоВыгрузка`, `АвтоЗагрузка`, `РучноеИспользование`, `СлужебныеФункции`, `СериализацияJSON`, `СериализацияXML`, `СериализацияAvroAsXml`, `ДесериализацияAvroAsXml`.

Экспортный API:

- **Общее** — `ВерсияПодсистемы`, `СообщитьПользователю`, `КорневаяИнформацияОбОшибке`.
- **Обмен** — `ВыгрузитьРегламент` / `ЗагрузитьРегламент` (точки входа заданий), `Выгрузить` / `Загрузить`, `ВыгрузитьПакетОтдельныйСеанс` / `ЗагрузитьРазделы` (точки входа фоновых заданий — обязаны оставаться экспортными с сериализуемыми параметрами).
- **Прямая работа из кода** — `КластерПоУмолчанию`, `Адаптер(Кластер)`, `СоздатьProducer` / `СоздатьConsumer` / `СоздатьAdmin`.
- **Служебное** — `ШинаИнфо`, `Тема`, `ОбновитьРегламентныеЗадания`, `ПолучитьГраничныеСмещенияТемыВФоне`, `СтрокаСоединенияИнфобазы`, `ЭтоФоновоеЗадание`, `ЭтоШаблон`, `ПолучитьРегулярноеВыражениеПоШаблону`.
- **Сериализация** — `JsonСериализовать` / `JsonДесериализовать`, `XmlСериализовать` / `XmlДесериализовать`, `AvroСериализовать` / `AvroДесериализовать` плюс низкоуровневые `AvroЗаписать*`.

Почти все входные процедуры начинаются с `УстановитьПривилегированныйРежим(Истина)`; модуль объявлен только серверным.

### Отправка сообщения идёт мимо адаптера

Единственное исключение из правила «весь ввод-вывод через `КафкаАдаптер`»: `ВыгрузитьОбъект` собирает `HttpЗапрос("producer/send")` сам, с телом в собственном версионированном двоичном формате (`BigEndian`, UTF-8), берёт соединение через `Адаптер.ПолучитьHttpСоединение()` и проверяет ответ через `Адаптер.ПроверитьHttpОтвет()`. Приём симметричен: `Адаптер.ConsumerPollRaw(..., "application/octet-stream")` и разбор того же формата. Несовпадение версии формата даёт ошибку «Неизвестная версия бинарной сериализации» — это признак несовместимого шлюза.

Ответ `producer/send` возвращается как структура `RecordMetadata` (`topic`, `partition`, `offset`, `timestamp`, `serializedKeySize`, `serializedValueSize`) и передаётся в `Обработка.ПослеВыгрузкиОбъекта`.

### Собственный XML-формат

`XmlСериализовать` покрывает типы, которые не умеет штатная сериализация платформы: `Структура` → `struct` со `structItem`, `Соответствие` → `map` с `mapItem` (ключ — любой сериализуемый тип), `Массив` → `array`, `ТаблицаЗначений` → `valueTable`. Остальное пишется штатными `ЗаписатьXML` / `ПрочитатьXML`.

Avro строится только в XML-представлении — двоичный Avro подсистема не формирует, документ отдаётся внешнему сериализатору. Область живая и экспортная, хотя из списка сердесов шины Avro убран.

## КафкаАдаптер

Обработка без форм и макетов. `Инициализировать(ШлюзАдрес, ШлюзПользователь, ШлюзПароль, ШлюзТаймаут, ЗащищенноеСоединение)` разбирает адрес по первому двоеточию (умолчание `localhost:8086`) и создаёт `HttpСоединение` с базовой аутентификацией.

Экспортные методы по группам: `Producer*` (создание, отправка, транзакции), `Consumer*` (подписка, назначение, перемотка, чтение, позиции), `Admin*` (кластер, темы, разделы, конфигурации, группы, смещения, SCRAM, ACL), `GetVersion`. Имена методов — CamelCase, пути — kebab-case (`AdminListEarliestOffsets` → `admin/list-earliest-offsets`).

**Модель ошибок.** Методы не выбрасывают исключений на ошибки шлюза и Kafka: `ПроверитьHttpОтвет` кладёт код в реквизит `КодОтвета`, тело ошибки в `ОписаниеОшибки` и возвращает `Неопределено`. Прикладной код обязан проверять результат на `Неопределено`. Сетевые ошибки и таймаут соединения при этом выбрасываются платформой как обычные исключения — `Попытка` в модуле нет нигде.

Клиент шлюза живёт по таймауту `expirationTimeout` и продлевается методами `*Touch`. Освобождать ресурсы (`ProducerRelease` / `ConsumerRelease` / `AdminRelease`) обязательно в `Попытка…Исключение`.

## Справочники

### КафкаКластеры

Точка подключения. Шлюз: `ШлюзАдрес` (умолчание `localhost:8086`), `ШлюзТаймаут` (умолчание 125 с), `ШлюзПользователь`, `ШлюзПароль` (`ХранилищеЗначения`). Kafka: `КафкаСерверы` → `bootstrap.servers`, `КафкаПротоколБезопасности` (`PLAINTEXT` / `SASL_PLAINTEXT` / `SASL_SSL` / `SSL`), `КафкаМеханизмSASL` (`PLAIN` / `SCRAM-SHA-256` / `SCRAM-SHA-512`), `КафкаПользовательУстановлен`, `КафкаПользователь`, `КафкаПароль`. Две табличные части свободных параметров — отдельно для отправителя и получателя.

`sasl.jaas.config` собирает подсистема. Логики в справочнике почти нет: `ПередЗаписью` требует механизм SASL при протоколе `SASL_*` и приводит имена параметров к нижнему регистру.

### КафкаШины

Один элемент — один однонаправленный обмен. Иерархический, два уровня; у группы есть только `ПользовательРеглЗадания`, элементы наследуют его через `ЕСТЬNULL(..., Родитель....)`.

Реквизиты: `Кластер`, `ОбменНаправление`, `Тема`, `Узел` (`ExchangePlanRef`), `КлючСердес` / `ЗначениеСердес`, `Обработка` (имя обработки обмена), `ПриемПроцедура`, `ОбменКоличествоПотоков`, `ОбменДлительностьСеанса`, `ПриемВРеальномВремени`, `ПриемТаймаутОжидания`, `КлиентТаймаут`, `ОбменПоРасписанию` + `ОбменРасписание` (`ХранилищеЗначения`) + `ОбменРасписаниеРазброс`, табличная часть `КафкаКонфигурация` (последний, самый приоритетный слой параметров клиента). Реквизиты `ОтправительИнфо` / `ОтправительОбъект` / `ПолучательИнфо` / `ПолучательОбъект` — чисто справочные, кодом не читаются.

Вся проверка целостности — в `ObjectModule.ПередЗаписью`: заполненность кластера и направления, допустимость сердесов по направлению (`Игнорировать` — только на приёме), правила имени темы, уникальность темы среди шин загрузки и узла среди шин одного направления, обязательность процедуры приёма, ненулевое число потоков, длительность сеанса при приёме в реальном времени, разброс не больше половины периода повтора.

`ПриЗаписи` и `ПередУдалением` вызывают `КафкаВызовСервера.ОбновитьРегламентныеЗадания` — задания живут автоматически.

## Позиции чтения

Регистр `КафкаПозиции`: измерения `Кластер`, `Тема`, `Раздел`, ресурс `Позиция`, реквизит `ДатаУстановки`. Экспортные методы менеджера: `ПолучитьПозицию`, `УстановитьПозицию` (период округляется вниз до часа, то есть не более одной записи в час на раздел), `УдалитьСтарые` (оставляет 100 последних записей, вызывается только для разделов, где за сеанс были сообщения).

Фиксация запрещена внутри активной транзакции, выполняется раз в 10 с и по окончании пачки. Позиция, вышедшая за границы раздела, подтягивается к ближайшей допустимой с предупреждением в журнал.

## Рабочее место администратора

Обработка `КафкаАдминистрирование`: модули объекта и менеджера пусты, вся логика — в форме `Основная` плюс модальные диалоги (`СозданиеТемы`, `ДобавлениеScramПользователя`, `ИзменениеScramПользователя`, `РедактированиеПозицииГруппы`, `УдалениеУчастниковГруппы`).

Устройство: почти весь код `&НаКлиенте`, состояние сессии — в клиентских переменных (`Admin`, `АдаптерПараметры`, `АдаптерКодОтвета`, `АдаптерОписаниеОшибки`). Каждая операция — пара «клиентский вызов → `&НаСервереБезКонтекста` обёртка», которая создаёт адаптер, дёргает один метод и возвращает результат либо `Неопределено`. Контекст формы на сервер не передаётся. Keep-alive — `AdminTouch` раз в 60 с.

Разделы: Кластер (свойства, узлы → конфигурация и каталоги), Темы (описание, разделы, конфигурация, смещения, данные, разрешённые операции), Группы (classic / consumer / share / streams), SCRAM-пользователи, Контроль доступа.

Диалоги ничего не делают сами — собирают ввод и возвращают структуру владельцу через `Закрыть(Результат)`.

## Документация

`docs/` — сайт MkDocs Material, публикуется на <https://kafka.savel.pro>. Разделы по адресату: `setup/` — внедренцу, `dev/` — разработчику обменов, `admin/` — администратору. Навигация задана вручную в `nav` файла `mkdocs.yml`; страница, не внесённая в `nav`, на сайт не попадёт и предупреждения не вызовет.

Окружение сборки пиновано в `requirements.txt`; `pygments-bsl` обязателен — без него подсветка примеров 1С молча пропадает. Автоматизации публикации в репозитории нет.

`overrides/main.html` — единственный шаблонный оверрайд, вручную добавляет Open Graph и Twitter Card метатеги.

## Известные шероховатости

Снимок наблюдений по коду, не проверявшийся на стенде. По мере исправления записи отсюда убираются.

**Гарантированные отказы в рабочем месте**

- Форма читает `Константы.КафкаКластерПоУмолчанию` напрямую, без привилегированного режима, а роль `КафкаАдминистрирование` прав на константу не даёт: пользователь только с этой ролью не откроет рабочее место. В ядре для этого есть `КафкаСервер.КластерПоУмолчанию()`, но она не вызывается ниоткуда.

**Прочее**

- `AvroЗаписатьEnum` и `AvroЗаписатьFixed` пишут атрибут `namespase`, а читается `namespace` — пространство имён теряется при обратном разборе. `AvroЗаписатьTimestampMicros` теряет микросекунды.
- В `Configuration.mdo` не объявлено ни одного языка (нет `<languages>`, нет `src/Languages`) при том, что все синонимы адресуют `ru`.
- `Configuration.distr` давно не обновлялся и содержит uuid, которым не соответствует ни один объект; новый объект в настройки поставки автоматически не попадёт.
- Имя процедуры приёма попадает в `Выполнить`: `ПолучитьСообщения` собирает `"Обработка." + ШинаИнфо.ПриемПроцедура + "(Сообщение)"` и исполняет строку в привилегированном режиме. Имя проверяется в `КафкаШины.ПередЗаписью` чёрным списком — пробел, обе скобки, `;` и `=`. По разбору грамматики этого хватает, чтобы строка осталась одним вызовом вида `Обработка.<цепочка свойств>.Метод(Сообщение)`, но достаточность держится на неочевидных свойствах языка: единственный разделитель операторов — `;`, голое обращение к свойству оператором не является, вызов требует скобок. Белый список кодов символов — как у имени темы семьюдесятью строками выше — снимал бы необходимость это доказывать при каждой правке.
- `ПолучитьПараметрыАдаптера` рабочего места возвращает на клиент расшифрованный пароль шлюза.
- `ЗаполнитьЗначенияПоУмолчанию` справочника `КафкаШины` объявлена экспортной, но не вызывается — кластер по умолчанию в новую шину не подставляется.
- `ProducerSend` объявляет обязательный параметр `ValueString` после параметра со значением по умолчанию.
- `docs/admin/workplace.md` описывает у темы закладку «Отправители», которой в форме нет.
- `КафкаВызовСервера/Module.bsl` — единственный `.bsl` без лицензионной шапки; модули пяти вспомогательных форм обработки администрирования тоже без неё.
